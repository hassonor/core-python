# Async I/O

Asynchronous programming with `asyncio` and `aiohttp`.

## Topics

- **[Asyncio Demos](demos_1/)** — Core async patterns including:
  - `async`/`await` basics
  - `asyncio.gather` for concurrent tasks
  - Async iterators
  - Converting synchronous code to async
  - HTTP requests with `requests` (sync) vs `aiohttp` (async)
- **[Async Stock Fetcher](stocks/)** — Real-world example comparing synchronous `requests` with async `aiohttp` for fetching stock data
